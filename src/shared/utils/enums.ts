export enum Mouse {
  LEFT_BUTTON = 0,
  RIGHT_BUTTON = 2,
}

export enum Points {
  MIN_POINTS = 0,
  MAX_POINTS = 6,
}

export enum Colors {
  DEFAULT = "#e0e0e0",
  GRAY = "gray",
  GREEN = "green",
  BLUE = "oklch(0.546 0.245 262.881)",
}

export enum Progress {
  EMPTY = 0,
  FULL = 100,
}
