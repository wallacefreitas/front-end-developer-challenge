import { BoardRoot } from "./BoardRoot";
import { BoardHeader } from "./BoardHeader";
import { BoardBody } from "./BoardBody";

export const Board = {
  Root: BoardRoot,
  Header: BoardHeader,
  Body: BoardBody,
};
